{
  "name": "reset-bug-repro",
  "version": "1.0.0",
  "description": "Reproducing a bug we had where a reset to the previous stage didn't reset the amount of plays for a player.",
  "main": "index.js",
  "scripts": {
    "start": "parcel index.html --open",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "browserslist": "defaults and supports async-functions",
  "author": "Xoeseko Nyomi",
  "license": "MIT",
  "dependencies": {
    "boardgame.io": "^0.49.12"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.5"
  }
}